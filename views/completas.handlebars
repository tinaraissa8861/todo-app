<main>
    <section class="tarefas">
        {{#each tarefas}}
        <div class="tarefa">
            <form action="">
                <div>
                    <input type="checkbox" id="tarefa-{{this.id}}" onchange="completarTarefa({{this.id}})">
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div>
                
                <button onclick="excluirTarefa({{this.id}})">
                    <img src="/imagens/x-icon.png" alt="icone de x para excluir uma tarefa">
                </button>
            </form>
        </div>
        {{/each}}
        <div class="informacoes">
            <p>{{quantidadeTarefas}} tarefas completas</p>

            <div class="botoes">
                <a href="/">Todas</a>
                <a href="/ativas" Ativas</a>
                <a href="/completas"class="active"></a>>Completas</a>
            </div>

            <a href="/limpartarefas">Limpar tarefa</a>
        </div>
    </section>

    <section class="botoes">
        <a href="/" >Todas</a>
        <a href="/ativas">Ativas</a>
        <a href="/completas" class="active"></a>Completas</a>
    </section>
</main>
